-----------------
Do zrobienia:
-----------------

sortowanie photos likes
upload files
role
wyszukiwanie po tagach
css

-----------------
Do wyjasnienia:
-----------------

#Formularz dodawania like jest, ale się nie zapisuje, nie wchodzi do if submitted (brak błędu z informacją)
#photoTag - problem z parametrem Tag
#upload photo - nie tworzy ścierzki i/lub twierdzi, że nie załączam pliku
#jak w edycji użytkownika wziąć Userdata dobrego formatu (żeby obecne dane były na wejściu)? (przekazanie przez parametr/ $userdata = $repository_data->findByUser($user->getId()); nie działa)


queryBuilder->join w Repository -> jak sortować po ilości like-ów?

Vouchers -> admin/autor - jak prościej niż:         if($this->getUser()->getId() ==$photo->getUser()->getId() or $this->isGranted('ROLE_ADMIN'))

mail(zdarzenia) -> upload - edit photo nie działa

