-----------------
Do zrobienia:
-----------------

sortowanie photos likes
upload files
role
wyszukiwanie po tagach
css

-----------------
Do wyjasnienia:
-----------------

Formularz dodawania like jest, ale się nie zapisuje, nie wchodzi do if submitted (brak błędu z informacją)
jak sortować po ilości like-ów?
jak w edycji użytkownika wziąć Userdata dobrego formatu? (przekazanie przez parametr/ $userdata = $repository_data->findByUser($user->getId()); nie działa)
photoTag - problem z parametrem Tag
upload photo - nie tworzy ścierzki i/lub twierdzi, że nie załączam pliku
upload - edit photo nie działa
unikalny login?
admin/autor - jak prościej niż:         if($this->getUser()->getId() ==$photo->getUser()->getId() or $this->isGranted('ROLE_ADMIN'))
