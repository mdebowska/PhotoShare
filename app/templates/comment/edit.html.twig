{% extends 'base.html.twig' %}

{% block title %}
    {{ 'comment_edit'|trans({'%id%': comment.id|default('')}) }}
{% endblock %}

{% block body %}
{% include 'default/_form.html.twig' with {
    page_title: 'comment_edit',
    page_title_param: {'%id%': comment.id},
    attr: {
        method: 'PUT',
        action: url('comment_edit', {'id': comment.id}),
    },
} %}
{% endblock %}